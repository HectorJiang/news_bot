name: Build & Deploy news_bot

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  # 1️⃣ 编译 Go 程序
  build:
    runs-on: ubuntu-latest
    container:
      image: hectorjiang/go-tdlib:latest  # 已经包含 TDLib
    steps:
      # 拉取仓库代码
      - uses: actions/checkout@v3

      # 下载 Go 依赖
      - name: Download Go dependencies
        run: go mod tidy

      # 编译二进制
      - name: Build news_bot
        run: go build -o news_bot main.go

      # 上传二进制供下一个 job 使用
      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: news_bot-binary
          path: news_bot

  # 2️⃣ 部署到宿主机
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      # 下载 build 阶段生成的二进制
      - name: Download binary
        uses: actions/download-artifact@v4
        with:
          name: news_bot-binary

      # SSH 到服务器，停止旧容器并启动新容器
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_SSH_PASSWORD }}
          script: |
            # 创建 TDLib 数据目录
            mkdir -p /opt/news_bot

            # 停止并删除旧容器
            docker stop news_bot || true
            docker rm news_bot || true

            # 将二进制拷贝到宿主机指定目录
            cp $GITHUB_WORKSPACE/news_bot /opt/news_bot/news_bot

            # 运行容器，挂载数据目录和二进制
            docker run -d \
              --name news_bot \
              -v /opt/news_bot:/app/tdlib-db \
              -v /opt/news_bot/news_bot:/app/news_bot \
              hectorjiang/go-tdlib:latest \
              /app/news_bot
